<ul>
<% @licences.each do |licence| %>
<li><%= licence.name %> <span><%= link_to t('dri.views.licences.buttons.edit'), edit_licence_path(licence.id) %> </span> </li>
<% end %>
</ul>

<%= link_to t('dri.views.licences.buttons.new'), new_licence_path, :id => "new_licence" %>
