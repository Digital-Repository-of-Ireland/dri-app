<ul>
  <li><%= link_to item.title, catalog_path(item) %> <%= button_to t('dri.views.collection.buttons.remove'), {:controller => :relationships, :action => 'delete', :id => item.id, :collection_id => collection_id }, :method => :delete, :class => 'btnRemove' %> </li>
</ul>
