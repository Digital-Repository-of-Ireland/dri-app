<div id="content" class="span9 show-document">
	<div id="document" class="document">
		<% if (params[:controller] == "collections" ) %>
		<h4><%= t('dri.views.objects.edit.title_collection') %></h4>
		<% else %>
		<h4><%= t('dri.views.objects.edit.title') %></h4>
		<% end %>
		<div class="dri_ingest_form_container">
			<%= form_for @object, as: :batch, :url => {:controller=>"collections", :action=>"update"}, :method=>"put", :html => {:class => 'dri_ingest_form'} do |f| %>

			<%= render :partial => "shared/form_metadata_qualifieddublincore", :locals => { :f => f, :action => "update" } %>
			<%= render :partial => "shared/access_controls", :locals => {:f => f,:record => @object } %>

			<%= f.submit t('dri.views.objects.buttons.save'), :class => "btn-primary btn-large", :id => "edit_collection" %>
			<% end %>

			<% if current_user.is_admin? || ((can? :manage_collection, @object) && @object.status.eql?('draft')) %>
			<%= form_tag (collection_path(@object.id)), :class=>'dri_ingest_form', :method => :delete, :style=>"margin:0;padding:0;display:inline;" do %>
			<%= submit_tag t('dri.views.collection.buttons.delete_collection'), :class => "dri_danger", :id => "delete_collection_#{@object.id}", :data => { :confirm => t('dri.views.collection.confirm_delete') } %>
			<% end %>
			<% end %>
			

			<div id='dri_collection_institutes_dropdown' name='dri_collection_institutes_dropdown' class='dri_associate_institutes_block'>
				<%= render :partial => "shared/associate_institute" %>
				<%= render :partial => "shared/add_institute" %>
			</div>


			<div id="dri_object_institutes_block" class="dri_object_institutes_block">
				<%= render :partial => "shared/display_institutes" %>
			</div>
		</div>
	</div>
</div>
