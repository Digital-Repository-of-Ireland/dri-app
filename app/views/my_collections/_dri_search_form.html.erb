
  <%= form_tag my_collections_index_path, :method => :get, :id => 'my_collections_searchform', :class => 'dri_workspace_search_input_form' do %>

    <% if search_fields.length > 1 %>
      <%= select_tag(:search_field,
                     options_for_select(search_fields, h(params[:search_field])),
                     title: t('blacklight.search.form.search_field.title'),
                     id: "search_field",
                     class: "custom-select search-field") %>
    <% elsif search_fields.length == 1 %>
      <%= hidden_field_tag :search_field, search_fields.first.last %>
    <% end %>

    <% if params[:mode].blank? %>
        <%= hidden_field_tag 'mode', 'objects' %>
    <% end %>

    <%= render_hash_as_hidden_fields(params_for_search().except(:q_ws, :search_field, :qt, :page, :utf8)) %>
    <!-- ID for the search box of 'q_text', to distinguish from date range search when rendering filter constraints in the UI -->
    <%= text_field_tag :q_ws, params[:q_ws], :placeholder => t('blacklight.search.form.q'), :id => "q_ws", :class => "dri_search_term_text_input dri_help_popover_slow", :'data-content' =>  t('dri.help.q'),  :'data-original-title' =>  t('dri.help.q_ws_title'), :'data-trigger' => "hover", :'data-placement' => "left" %>
    <button type="submit" class="dri_search_term_text_submit_button dri_no_display" id="search">
      <%=t('blacklight.search.form.submit')%>
    </button>

  <% end %><!-- end form -->
<!-- close dri_search_term_input_form -->
