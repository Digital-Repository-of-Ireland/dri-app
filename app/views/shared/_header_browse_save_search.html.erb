<% if search_state.has_constraints? && session[:history].present? && current_user && (params[:controller] == 'catalog' && params[:action] == 'index') %>
    <div id="dri_search_save_input_id" class="dri_sort_nav">
      <%= button_to(t('dri.views.objects.buttons.save_current_search'), save_search_path, method: :put, class: "dri_search_save_submit_button", id: 'save_search')%>
    </div>
<% end %>
