<%= form_for(@inst, :url => institutes_path(:format => :js), remote: true) do |f| %>
  <%= hidden_field_tag 'object', @object.id %>
  <%= f.label :name %>:
  <%= f.text_field :name %><br />

  <%= f.file_field :logo %>

  <%= f.submit t('dri.views.collection.institute.add') %>
<% end %>
