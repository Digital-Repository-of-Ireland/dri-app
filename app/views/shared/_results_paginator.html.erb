<% if has_search_parameters? && params[:view] != "timeline" && params[:view] != "maps" && params[:action] != 'facet' %>
    <% if @response.total_pages > 1 %>
        <%= paginate @response, :window => 1, :theme => 'blacklight' %>
    <% end %>
<% end %>
