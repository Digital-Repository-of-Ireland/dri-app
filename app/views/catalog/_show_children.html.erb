
	<!-- Display Tree Structure, for EAD collection objects only -->

	<% # Showing only the first 10 results %>
	<% tree_html = show_ead_tree(@document, "4") %>
	<% if (!tree_html.blank?) %>
	<% # PLACE HERE THE DIV AND STYLING %>
	<%= tree_html.html_safe %>
	<% if (count_immediate_children_in_collection(@document['id']) > 3) %>
	MORE
	<% # ADD SHOW_MORE LINK HERE %>
	<% end %>
	<% end %>
<div class="dri_collection_image">
						
</div>
